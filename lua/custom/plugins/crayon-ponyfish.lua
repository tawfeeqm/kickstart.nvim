-- lua/custom/plugins/crayon-ponyfish.lua
return {
  {
    'rktjmp/lush.nvim',
    -- lazy = false,
    -- priority = 1000,
    config = function()
      -- This line loads the file you created in Step 2
      -- local theme = require 'lush_theme.crayon-pony-fish'

      -- This line applies the colors
      -- require 'lush'(theme)
    end,
  },
}
